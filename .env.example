# Connection string do Postgres (Neon). Obrigatória para a API.
DATABASE_URL='postgresql://user:password@host/dbname?sslmode=require'
# Segredo HMAC dos tokens de sessão (ex.: openssl rand -hex 32).
AUTH_SECRET='troque-por-um-segredo-aleatorio'
